{
  "scripts": {
    "dev": "concurrently --kill-others \"npm --prefix frontend/ run dev\" \"(cd backend/ && npx hardhat node)\"",
    "deploy-dev": "(cd backend/ && npx hardhat run scripts/deploy.ts --network localhost)"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}